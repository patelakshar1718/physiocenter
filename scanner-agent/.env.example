# Paste the device token issued from the admin web app's Devices page here.
DEVICE_TOKEN=

# Base URL of the PhysioCenter server's API.
API_URL=http://localhost:4000/api/v1

# How often (ms) to retry flushing any queued offline scans.
OFFLINE_RETRY_INTERVAL_MS=15000
